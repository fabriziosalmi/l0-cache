import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Minimal, macOS-native look: the styling lives entirely in custom.css (it just
// overrides VitePress design tokens + a few component touches), so the default
// theme's behavior is preserved.
export default DefaultTheme
