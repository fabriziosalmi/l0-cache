# Tokenizer-measured savings

Generated by `cargo run --release --example token_benchmark -- --write` with l0-compressor `0.2.0` at default settings (`--no-auto`, head 30 / tail 30 / tail-error 120 / threshold 100). Token counts are real tokenizer runs (tiktoken `o200k_base` and `cl100k_base`), not the bytes/4 estimate used by the built-in telemetry. Fixtures are captured real command outputs, committed under `benchmarks/fixtures/` for reproducibility; the service log is synthetic (generated) and replayed with exit 1 to exercise the error-tail path.

| Fixture | Lines | o200k tokens raw → filtered | saved | cl100k saved |
|---|---|---|---|---|
| cargo-test.txt (cargo test (354 tests, workspace)) | 373 → 22 | 5360 → 189 | **96.5%** | 96.5% |
| cargo-build.txt (cargo build --release (clean, 33 crates)) | 30 → 2 | 387 → 27 | **93.0%** | 93.0% |
| git-log-stat.txt (git log --stat -120) | 1390 → 66 | 18439 → 807 | **95.6%** | 95.7% |
| git-show-diff.txt (git show <commit> (code diff)) | 429 → 42 | 5203 → 445 | **91.4%** | 91.4% |
| npm-ls.txt (npm ls --all (vitepress tree)) | 222 → 42 | 4388 → 536 | **87.8%** | 87.6% |
| service-log-fail.txt (service log dump, 5100 lines, exit 1) | 5100 → 156 | 161759 → 4716 | **97.1%** | 97.1% |
| **Total (weighted)** | | 195536 → 6720 | **96.6%** | 96.6% |

Per-fixture range **88–97%**, median **96%** (o200k_base). Outputs below the 100-line threshold are never head/tail-truncated (line collapsing still applies — see the cargo-build row), and short outputs are not in this corpus: these numbers describe noisy outputs, which is where the tool is meant to sit. Session-wide savings depend on the command mix; the built-in `--stats` telemetry reports 50–80% on typical mixed sessions.
